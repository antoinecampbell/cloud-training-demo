apiVersion: v1
kind: Service
metadata:
  name: ui-service
  labels:
    app: ui-service
spec:
  selector:
    app: ui
  type: NodePort
  ports:
    - port: 80
      nodePort: 30080
      protocol: TCP