spring:
  application:
    name: note-service

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config:8888

eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka
  instance:
    prefer-ip-address: true